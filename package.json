{
  "name": "tlbot-standalone",
  "version": "1.0.0",
  "description": "Telegram bot for TL Clothing",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "npx prisma generate && node index.js",
    "dev": "node --watch index.js",
    "deploy": "npx prisma generate && npx prisma migrate deploy && node index.js"
  },
  "dependencies": {
    "telegraf": "^4.16.3",
    "@prisma/client": "^6.13.0",
    "prisma": "^6.13.0",
    "dotenv": "^17.2.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
